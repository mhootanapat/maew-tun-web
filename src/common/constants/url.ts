// IMPORTANT: Add trailing slash for redirect url without /index.html on server

export const MAIN_URL = Object.freeze({
    //error
    error: Object.freeze({
      main: '/error/',
    }),
    //home
    home: Object.freeze({
      main: '/',
    }),
  });
  